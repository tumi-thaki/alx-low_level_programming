#include <stdio.h>
/**
 * main - entry point, prints numbers from 0 to 
 * Return: 0 always for success
*/
int main(void)
{
	int num = 0;
	for (num = 0; num < 9; num++)
	       printf("%d", num);
	putchat('\n');
	return (0);
}
